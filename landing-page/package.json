{
  "name": "oncallm",
  "version": "1.0.0",
  "description": "An LLM-powered agent that helps with Kubernetes debugging based on alerts received from monitoring systems.",
  "scripts": {
    "dev": "vitepress dev ../docs --port 5174",
    "build": "vitepress build ../docs --outDir ./dist",
    "preview": "vitepress preview ../docs --outDir ./dist",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "keywords": ["kubernetes", "ai", "alerts", "monitoring", "oncall", "devops"],
  "author": "OncaLLM Team",
  "license": "MIT",
  "homepage": "https://oncallm.github.io/oncallm",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vitepress": "^1.0.0-rc.31"
  }
}
